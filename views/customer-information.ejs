<!DOCTYPE html>
<html>
<head>
    <title>Customer Information</title>
    <link rel="stylesheet" type="text/css" href="/styles.css">
    <script src="/search_box.js"></script>
</head>
<body>
<div class="wrapper">
    <%- include('partials/sidebar') %>
    <div class="main_content">
        <div class="header" style="text-transform: uppercase;"><h2>Customer Information</h2></div>
        <div class="info">
            <div class="table">
                <section class="table__header">
                    <h2>Customer Information Table</h2>
                    <div class="input-group">
                        <input type="search" id="searchData" name="searchData" placeholder="Search Data...">
                        <i class="fa fa-search" aria-hidden="true"></i>
                    </div>
                </section>

                <table class="content-table">
                    <thead>
                        <tr>
                            <th>First Name <span class="icon-arrow">&#11014;</span></th>
                            <th>Last Name <span class="icon-arrow">&#11014;</span></th>
                            <th>Phone Number <span class="icon-arrow">&#11014;</span></th>
                            <th>Email Address <span class="icon-arrow">&#11014;</span></th>
                            <th>Birthday <span class="icon-arrow">&#11014;</span></th>
                            <th>Last Visited Date <span class="icon-arrow">&#11014;</span></th>
                        </tr>
                    </thead>
                    <tbody>
                        <% customers.forEach(function(customer) { %>
                            <tr data-phoneNumber="<%= customer.phoneNumber %>">
                                <td><%= customer.firstName %></td>
                                <td><%= customer.lastName %></td>
                                <td><%= customer.phoneNumber %></td>
                                <td><%= customer.emailAddress %></td>
                                <td><%= customer.birthday %></td>
                                <td><%= customer.lastVisit %></td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>

        </div>
    </div>
</div>
</body>
</html>
